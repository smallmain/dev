# 多语言文档

本仓库指定 [维护范围](#维护范围) 的文档需同时维护以下几种语言的版本：

1. 英文（`.`）
2. 简体中文（`.zh`）

## 约定

- 文档可先用任何语言进行撰写，再翻译成其它语言；但表达的内容必须一致。
- 同一份文档的多个版本文件必须存放在同一目录，使用不同的后缀。

  - 不同语言所使用的后缀必须与 [多语言文档](#多语言文档) 中有序列表括号内的一致。

  例如：

  - 英文：`foo.md`
  - 中文：`foo.zh.md`
  - 英文：`specs/web/coding.md`
  - 中文：`specs/web/coding.zh.md`

- `i18n/consistency.yaml` 文件记录每个版本的 Git blob hash，当确认两份文档内容一致时，更新该文件。

  例如：

  ```yaml
  foo.md: 3f786850e387550fdab836ed7e6dc881de23001b
  foo.zh.md: 89e6c98d92887913cadf06b2adb97f26cde4849b
  specs/web/coding.md: 0fdab83d92887913cadf06b2adb97f26cde4849c
  specs/web/coding.zh.md: 89e6c980fdab8313cadf06b2adb97f26cde4849d
  ```

## 结构

- **多语言跳转行**：每篇文档需存在类似 `[其它语言](...) | 当前语言 | ...` 的单独行方便跳转至其它语言版本。

  - 如果某个版本的文档不存在跳转行，则统一在 H1 标题后添加跳转行。
  - 语言切换行的多个语言的顺序必须是固定的，与 [多语言文档](#多语言文档) 中的有序列表顺序一致，且使用相同的语言名称。

  例如：

  中文版本：

  ```markdown
  # Foo

  [English](foo.md) | 简体中文

  ## Bar

  ...
  ```

  英文版本：

  ```markdown
  # Foo

  English | [简体中文](foo.zh.md)

  ## Bar

  ...
  ```

## 翻译

- 翻译前必须通过对比 Git blob hash 来判断哪份语言的文档是新的、哪份是旧的；如果只有一种语言的版本存在，即直接将其翻译成其它语言。
- 当存在已确认一致内容时，不要重新翻译整篇文档的内容，通过 Git blob hash 获取新文档的差异内容，并只同步翻译差异内容。
- 所谓 “不同语言版本的文档表达的内容要一致” 是指：

  - 文档结构（标题结构、列表类型、表格行列数、段落等等）必须一一对应。
  - 文档表达的意义必须一致。

## 维护范围

- `*.md`
- `*.md.ejs`

排除文档：

- `AGENTS.md`

# 验证

- `sm check` 是本仓库正在开发的产品功能，不得将 `sm check` 用作本仓库的通用验收命令。
